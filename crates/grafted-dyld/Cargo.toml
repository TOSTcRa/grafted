[package]
name = "grafted-dyld"
description = "dyld emulation — shared cache parsing, image loading, dylib resolution"
version.workspace = true
edition.workspace = true

[dependencies]
goblin.workspace = true
memmap2.workspace = true
thiserror.workspace = true
log.workspace = true
libc = "0.2"
grafted-loader = { path = "../grafted-loader" }
grafted-objc = { path = "../grafted-objc" }
